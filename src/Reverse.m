function reversedArray = Reverse(arrayToReverse)
    % Reverses a 1D input array
    % Input: A 1D array
    % Output: the input array, but reversed
    % Author: Connor Mattson
    % Version: 1
    % Date: 25/08/2017
    % ENGGEN 131, Matlab Project
    
    reversedArray = flip(arrayToReverse, 2);
end